/**
 * Repositorio del módulo Asistencia Técnica.
 * Fase 0: stub — sin queries reales todavía.
 * La implementación real va en la Fase B.
 */

// El repositorio se implementará en la Fase B con las queries de Prisma.
// Se declara aquí para que el service pueda importarlo sin errores de compilación.

export const assistanceRepository = {
  // Fase B: findSessionById, createSession, listSessions, etc.
};
