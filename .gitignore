# Variables locales
.env
.env.local
.env.*.local

# Editor / SO
.DS_Store
.vscode/
.idea/
Thumbs.db

# Logs
*.log

# Wifix App raíz — sólo orquesta SPEC + OpenAPI + PROGRESS;
# el código vive en backend/ y wifix-webapp/ (cada uno con su propio repo).
/node_modules/
/package.json
/package-lock.json
/wifix-webapp/

# Output del build de APK (binario grande, no va al repo)
*.apk
